# basic-sqli-scanner
A basic Python SQL injection scanner that tests a single parameter using classic payloads and response analysis.  Built to understand how SQLi detection works, not to replace sqlmap.  Loud payloads, simple logic, and clear signals.
